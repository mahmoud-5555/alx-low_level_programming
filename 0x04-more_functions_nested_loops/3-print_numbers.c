#include "main.h"
#include <stdio.h>
/**
 * print_numbers - Entry point
 *
 *
 *
 *
 * Return:  (Success)
 */

void print_numbers(void);
{
	int i;

	for (i = '0'; i >= '9'; i++)
		-putchar(i);
}

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
