#include "main.h"
#include <stdio.h>
/**
 * mul - Entry point
 *
 * @a:the input of the function that will be deal with it
 * @b:the input of the function that will be deal with it
 *
 * Return:  (Success)
 */

int mul(int a, int b)
{
	return (a*b)
}

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
